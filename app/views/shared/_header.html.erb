<div class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to "Paranoia Computer", root_path, class: "navbar-brand" %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            <i class="fa fa-bullseye"></i> Missions
          </a>
          <ul class="dropdown-menu">
            <% if @missions.any? %>
              <% @missions.each do |m| %>
                <li><%= link_to "Mission #{m}", voice_lines_path(m: m) %></li> 
              <% end %>
            <% else %>
              <li><a href="#"><em>No missions</em></a></li>
            <% end %>
          </ul>
        </li>
        <li>
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            <i class="fa fa-volume-up"></i> Lines
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to "<i class=\"fa fa-list-ul\"></i> Manage".html_safe, voice_lines_path %></li>
            <li><%= link_to "<i class=\"fa fa-plus\"></i> New line".html_safe, new_voice_line_path %></li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</div>
